{
  "name": "rutorrent-dracula-theme",
  "version": "0.2.1",
  "private": true,
  "description": "Development tooling for the Dracula theme for ruTorrent. Nothing here ships — the theme is the Dracula/ folder, copied into ruTorrent as it is.",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "lint:css": "stylelint \"Dracula/**/*.css\"",
    "lint:js": "eslint .",
    "lint": "npm run lint:css && npm run lint:js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run format:check && npm run lint && npm test",
    "deploy": "./deploy.sh",
    "deploy:5.2.0": "STAND_CONTAINER=rutorrent-b520 STAND_DATA=tmp/matrix/b-5.2.0/data ./deploy.sh",
    "deploy:5.1.12": "STAND_CONTAINER=rutorrent-b5112 STAND_DATA=tmp/matrix/b-5.1.12/data ./deploy.sh",
    "deploy:older": "npm run deploy:5.2.0 && npm run deploy:5.1.12",
    "verify": "npm run check && npm run deploy",
    "verify:older": "npm run check && npm run deploy:older"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.1",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "stylelint": "^17.14.1",
    "stylelint-config-recommended": "^18.0.0",
    "stylelint-declaration-strict-value": "^1.12.1"
  }
}
